[phases.setup]
nixPkgs = ["dotnet-sdk_10"]

[phases.install]
cmds = ["cd GymApp.API && dotnet restore"]

[phases.build]
cmds = ["cd GymApp.API && dotnet publish -c Release -o out"]

[start]
cmd = "cd GymApp.API && dotnet GymApp.API.dll"